<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
<img class="logo"
     src="{{ asset('assets/uploads/logo.png')}}"
     alt="a"
    >
	 		<a class="navbar-brand" href="{{ path('main') }}">ResinArt.Ola</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('main') }}">Accueil</a>
				</li>
			
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Boutique
					</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						<li>
							<a class="dropdown-item" href="{{ path('categories_list', {"slug": 'arts' }) }}">Arts</a> 
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('categories_list', {"slug": 'pierres' }) }}">Pierres</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{ path('categories_list', {"slug": 'accessoires' }) }}">Accessoires</a>
						</li>
					</ul>
				</li>
					<li class="nav-item">
					<a class="nav-link" href="{{ path('app_about') }}" style="color: rgb(244, 164, 164)";>Qui sommes nous</a>
				
				</li>
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_contact') }}">Contactez-moi</a>
				</li>
			</ul>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				{% if app.user %}
                    {% if is_granted('ROLE_PRODUCT_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_index') }}">Administration</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('profile_index') }}">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}"style="color: rgb(244, 164, 164)";>Me déconnecter</a>
                    </li>
					<li class="nav-item">
    <a href="{{ path('cart_index') }}" >
        <img src="{{ asset('assets/uploads/cart4.svg') }}" alt="Panier" style="width: 30px; height: 30px; fill: #FF69B4;">
    </a>
</li>

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}"style="color: rgb(244, 164, 164)";>M'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}"style="color: rgb(244, 164, 164)";>Me Connecter</a>
                    </li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>